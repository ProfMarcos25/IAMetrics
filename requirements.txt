# ── API / Servidor ────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.12

# ── Banco de Dados (psycopg v3) ───────────────────────────
psycopg[binary]>=3.1.0

# ── Visão Computacional / IA ──────────────────────────────
# dlib deve ser instalado ANTES do face_recognition
# Windows: instale CMake + Visual C++ Build Tools antes do pip install
dlib>=19.24.0
face_recognition>=1.3.0
# Modelos treinados usados internamente pelo face_recognition (dlib .dat files)
# Ref: https://github.com/ageitgey/face_recognition_models
face_recognition_models>=0.1.0
opencv-python>=4.9.0
numpy>=1.26.0

# ── Utilitários ───────────────────────────────────────────
python-dotenv>=1.0.1

# ── Notificações ─────────────────────────────────────────
twilio>=9.1.0
python-telegram-bot>=21.1.1